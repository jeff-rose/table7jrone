---
- name: something
  hosts: localhost
  vars:
    group: table1jrrg2
    webapp_name: table1jrwebapp
    location: westus
  tasks:
    - name: create rg
      azure_rm_resourcegroup:
        name: "{{group}}"
        location: "{{location}}"
    - name: create app svc
      azure_rm_webapp:
        resource_group: "{{group}}"
        name: "{{webapp_name}}"
        plan:
          resource_group: "{{group}}"
          name: table1jrappplan
          is_linux: true
          sku: S1
          number_of_workers: 1
        frameworks:
          - name: "java"
            version: "8"
            settings:
              java_container: tomcat
              java_container_version: 8.5
              
